{
    "SplitTunnelParam": {
        "Type": "String",
        "Default": "true",
        "Description": "Set to FALSE if you want all the traffic to be sent over the VPN. If set to TRUE, only partial traffic will go through the VPN, the traffic for the local network of the VPN.",
        "AllowedValues": [
            "true",
            "false"
        ]
    }
}